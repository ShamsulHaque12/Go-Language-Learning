package main

/*
import "fmt"

type Person struct {
	Name string
	Age  int
}

type Employe struct {
	Person
	Position string
	Salary   float64
}

func (e Employe) ShowInfo() string {
	return fmt.Sprintf("Name: %s, Age: %d, Position: %s, Salary: %.2f", e.Name, e.Age, e.Position, e.Salary)
}

func main() {
	fmt.Println("========== Type Embedding ==========")

	employe := Employe{
		Person: Person{
			Name: "Shamsul Haque",
			Age:  22,
		},
		Position: "Software Engineer",
		Salary:   100000,
	}

	fmt.Println(employe.ShowInfo())

}

*/

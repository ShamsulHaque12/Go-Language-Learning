package main

/*

import (
	"fmt"
	"os"
)

func main() {
	fmt.Println("===== Terminal Environment Variables =====")

	// 1. Get PORT from env (with default fallback)
	port := os.Getenv("PORT")
	if port == "" {
		port = "3000"
	}

	// 2. Get DB_USER from env
	dbUser := os.Getenv("DB_USER")
	if dbUser == "" {
		dbUser = "guest"
	}

	// 3. Get APP_ENV from env
	appEnv := os.Getenv("APP_ENV")
	if appEnv == "" {
		appEnv = "development"
	}

	// 4. Using os.LookupEnv to check if key exists
	dbPass, exists := os.LookupEnv("DB_PASS")
	if !exists {
		dbPass = "secret_default"
	}

	fmt.Println("PORT:    ", port)
	fmt.Println("DB_USER: ", dbUser)
	fmt.Println("APP_ENV: ", appEnv)
	fmt.Println("DB_PASS: ", dbPass)
}

*/
